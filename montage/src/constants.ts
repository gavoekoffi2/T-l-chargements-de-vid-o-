export const VIDEO_FPS = 30;
export const VIDEO_WIDTH = 1080;
export const VIDEO_HEIGHT = 1920;

// Durées en frames (30fps)
export const INTRO_DURATION = 90;       // 3s
export const MAIN_DURATION = 8394;      // ~279.8s — vidéo source complète
export const OUTRO_DURATION = 90;       // 3s
export const TOTAL_DURATION = INTRO_DURATION + MAIN_DURATION + OUTRO_DURATION;

// Palette couleurs — style moderne / premium
export const COLORS = {
  primary: "#00D4FF",       // cyan vif
  secondary: "#FF3CAC",     // magenta
  accent: "#784BA0",        // violet
  gold: "#FFD700",          // or
  dark: "#0A0A0F",          // noir profond
  darkBlue: "#0D0D2B",      // bleu nuit
  white: "#FFFFFF",
  lightGray: "#E0E0E0",
};

// Typographie
export const FONTS = {
  heading: "'Inter', sans-serif",
  body: "'Inter', sans-serif",
};
